# 페이스메이트 Codex 작업 지침

## 프로젝트 목표

이 프로젝트는 사용자가 목표와 실행 가능한 시간을 입력하면 실행 궁합이 맞는 한 사람과 연결해 주는 서비스 `페이스메이트`를 기획하고 검증한다.

현재 첫 상품은 만 19세 이상 성인을 위한 온라인 `7일 공부짝`이다. 특정 직업이나 개발자로 사용자를 제한하지 않는다. 장기적으로 다른 반복 목표로 확장할 수 있지만, 첫 상품의 검증 전에는 범위를 넓히지 않는다.

## 문서의 우선순위

- 현재 제품 정의의 단일 기준 문서는 `docs/goal-partner-product-plan.md`다.
- `archive/`는 아이디어 탐색 과정의 기록이다. 현재 요구사항이나 결정으로 취급하지 않는다.
- 새로운 핵심 제품 문서를 만들기 전에 기존 기획서에 통합할 수 있는지 먼저 판단한다.
- Codex 운영 파일은 `.codex/`에 두고 `docs/`에는 제품 핵심 문서만 둔다.

## 작업 원칙

- 사용자의 실제 문제, 반복 사용 가능성, 매칭 밀도, 참여율을 기능 수보다 우선한다.
- 가설과 확인된 사실을 구분한다. 조사 결과에는 출처와 조사 날짜를 남긴다.
- 최신 정보, 법률, 개인정보, 결제, 플랫폼 정책은 공식 1차 자료를 우선 확인한다.
- 사용자가 구현을 명시적으로 요청하기 전에는 기획 검증을 코드 구현으로 확대하지 않는다.
- 중요한 제품 결정은 이유, 반대 근거, 검증 방법과 함께 기획서에 반영한다.
- 기존 사용자 변경을 보존하고 요청 범위 밖의 파일은 수정하지 않는다.

## 서브에이전트 사용

- 상세 운영 원칙은 `.codex/SUBAGENT_STRATEGY.md`를 따른다.
- 시장 조사, 반대 관점 검토, 기술 타당성처럼 독립적으로 분리되는 작업에만 서브에이전트를 사용한다.
- 단순 문서 수정이나 하나의 짧은 질문에는 서브에이전트를 사용하지 않는다.
- 동시에 최대 3개까지만 실행하며, 각 에이전트에는 하나의 명확한 산출물만 맡긴다.
- 여러 에이전트가 같은 파일을 수정하게 하지 않는다. 기본적으로 서브에이전트는 조사 결과만 반환하고, 최종 판단과 파일 수정은 메인 에이전트가 담당한다.
- 서브에이전트 결과를 그대로 채택하지 말고 근거, 중복, 충돌을 검토해 하나의 결론으로 통합한다.

## 완료 기준

- 요청한 결과가 실제 파일에 반영되어 있다.
- 문서 구조, 링크, 용어의 일관성을 확인했다.
- 사실 주장과 제품 가설이 구분되어 있다.
- 중요한 미확정 사항과 다음 검증 행동이 명시되어 있다.
- 코드가 생긴 이후에는 관련 테스트와 검사 명령을 확인하고 이 문서에 추가한다.

